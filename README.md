# Hack Assembler
An assembler for the Hack machine language specification, written in Python. Run it as a regular Python script in the command line, supplying some *.asm file. Note that this assumes any Assembly script supplied is well-written (i.e. conforms to the Hack machine language specification). 
